apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: db:5432
    user: ivan
    database: sensordb
    secureJsonData:
      password: qwerty
    jsonData:
      sslmode: "disable"
      postgresVersion: 15
      timescaledb: false
